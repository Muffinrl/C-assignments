/*
    This program corresponds to assignment 2.12: The program is_prime.
*/

#include <stdio.h>

int main(void) {
    return 0;
}

/*
    isPrime() takes an input value a of type int.
    it proceeds to go through all its divisors, adding every "succesfull" division to the auxiliary variable i.
    i starts at -2, meaning that if a has 2 divisors, i will be 0.
    This is a clean way to check for 2 divisors. As 0 corresponds with false, thus the output will be i == 0.
*/
int isPrime(int a){

}
